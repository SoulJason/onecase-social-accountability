# The React Native / Expo ecosystem routinely has peer-dependency ranges that
# npm's strict resolver rejects (especially across React major versions).
# legacy-peer-deps tells npm to install anyway, which is the standard and safe
# choice for Expo projects.
legacy-peer-deps=true
